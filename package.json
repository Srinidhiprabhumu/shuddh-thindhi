{
  "name": "shuddh-thindi-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "client-app",
    "server-app"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client-app && npm run dev",
    "dev:server": "cd server-app && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client-app && npm run build",
    "build:server": "cd server-app && npm run build",
    "install:all": "npm install && cd client-app && npm install && cd ../server-app && npm install",
    "clean": "rm -rf node_modules client-app/node_modules server-app/node_modules client-app/dist server-app/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}